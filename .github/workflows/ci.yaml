name: learn-github-actions
on:
  push:
    branches:
      - main

jobs:
  my_first_action:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: |
          echo "Hello, world!"

      - name: Another step
        run: |
          echo "This is another step."

      - name: Final step
        run: |
          echo "This is the final here."
